<p id="notice"><%= notice %></p>

<p>
  <strong>Nome da Sala:</strong>
  <%= @room.description %>
</p>

<br>
<br>
<p>Agenda da sala</p>
<table class="table table-striped">
  <tr>
    <th>Horário</td>
    <th><%= days_of_week[0] %></th>
    <th><%= days_of_week[1] %></th>
    <th><%= days_of_week[2] %></th>
    <th><%= days_of_week[3] %></th>
    <th><%= days_of_week[4] %></th>
    <th><%= days_of_week[5] %></th>
    <th><%= days_of_week[6] %></th>
  </tr>

    <% Constants::HOURS.each do |hour| %>
      <tr>
        <td class="col-md-1"><%= hour %></td>
        <td class="col-md-1"><%= link_to check_situation(@room, days_of_week[0], hour), schedule_room_path(@room.id, hour, @current_user.id), :remote => true %></td>
        <td class="col-md-1"><%= link_to check_situation(@room, days_of_week[1], hour), schedule_room_path(@room.id, hour, @current_user.id), :remote => true %></td>
        <td class="col-md-1"><%= link_to check_situation(@room, days_of_week[2], hour), schedule_room_path(@room.id, hour, @current_user.id), :remote => true %></td>
        <td class="col-md-1"><%= link_to check_situation(@room, days_of_week[3], hour), schedule_room_path(@room.id, hour, @current_user.id), :remote => true %></td>
        <td class="col-md-1"><%= link_to check_situation(@room, days_of_week[4], hour), schedule_room_path(@room.id, hour, @current_user.id), :remote => true %></td>
        <td class="col-md-1"><%= link_to check_situation(@room, days_of_week[5], hour), schedule_room_path(@room.id, hour, @current_user.id), :remote => true %></td>
        <td class="col-md-1"><%= link_to check_situation(@room, days_of_week[6], hour), schedule_room_path(@room.id, hour, @current_user.id), :remote => true %></td>
      </tr>
    <% end %>
</table>
